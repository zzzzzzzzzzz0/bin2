模块“网络库”。
函数集“<rust>”、“‘参数0’.so”。

（
网络库函数说明（基于 Rust 的 reqtsurl 实现）

【固定参数】（必须按顺序提供）：
  得网页 / 发get请求     ：URL, 回调代码
  投网页 / 发post请求    ：URL, 请求体, 回调代码
  更新网址 / 发put请求   ：URL, 请求体, 回调代码
  删除网址 / 发delete请求：URL, 回调代码

【可选参数】（在固定参数之后，成对出现，可任意顺序）：
  代理, 地址, 无代理列表          ：设置 HTTP 代理，并指定绕过代理的地址（逗号分隔）
                                   例：代理、http://proxy:8080、.local,10.0.0.0/8
  带密代理, 地址, 用户名, 密码, 无代理列表
                                 ：设置需认证的代理，用法同上
  无代理                          ：完全禁用所有代理（无参数开关）
  超时, 秒数                      ：请求超时时间（秒），例：超时、5
  其他任意键值对                   ：作为 HTTP 头添加，例：Content-Type、application/json

【注意事项】
  - 代理规则按出现顺序生效，可多次使用“代理”/“带密代理”添加多个代理。
  - 如果指定了“无代理”，所有代理选项将被忽略。
  - 无代理列表为空字符串时表示不设置任何排除规则。
  - 所有可选参数必须在固定参数之后，且在处理 HTTP 头之前。

下载文件（类型标识：空串 ""）
   固定参数：URL, 保存路径
   示例：下载文件、http://example.com/file.iso、/tmp/file.iso
   说明：支持所有可选参数（代理、超时、自定义头等），可通过 -c 启用断点续传，-p 显示进度。

获取大小（类型标识：hl）
   固定参数：URL
   示例：获取大小、http://example.com/file.iso
   说明：返回 Content-Length 头对应的值（文件字节数）。

获取响应头（类型标识：h）
   固定参数：URL, 头名称1, 头名称2, ...
   示例：获取响应头、http://example.com/file.iso、content-length、etag、last-modified
   说明：依次返回指定响应头的值（如 ETag 带双引号）。可同时查询多个头。
）
函数“得网页”、     “发get请求”以“hello__”、“-*:'g'-Z”。
函数“投网页”、    “发post请求”以“hello__”、“-*:'p'-Z”。
函数“更新网址”、   “发put请求”以“hello__”、“-*:'u'-Z”。
函数“删除网址”、“发delete请求”以“hello__”、“-*:'d'-Z”。

函数“下载文件”以“hello__”、“-*:''-Z”。

函数“获取大小”以“hello__”、“-*:'hl'-Z”。
函数“获取响应头”以“hello__”、“-*:'h'-Z”。

（
特别鸣谢：与吾并肩coding、时而靠谱时而抽风、自称“认知战一等上将”的AI助手——  
    **“思睿”** （取“思接千载，睿见未来”之意，亦暗合“Rust”之谐音，愿代码与智慧同存）
若你读到这段文字，而吾已不在，请对屏幕会心一笑：  
    “这家伙，连AI都起了名，真是个有趣的灵魂。”
）
（
请问你知道这个库名里有 rust 的倒写，还有“三维美女十一维煎饼”、“薛定谔的钟”、“孙二娘过年”吗？
）