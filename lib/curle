（必须跟curl库合用，所用地址来自其的 curl_easy_init 亦须 curl_easy_perform 来执行）
模块“curl二库”。
函数集“‘参数0’.so”。

函数“加头”以“head__”、“i-&-Z”（地址、……）。
函数“释放头”以“head_free__”。

函数“打开写”以“write_open__”、“i-&-c*-i”（地址、保存路径（类型为0、2、3时）、类型（0、1（放在内存）、2、3（用于继传）））。
函数“获取写”以“write_get__”、“c*”。
函数“关闭写”以“write_close__”。
