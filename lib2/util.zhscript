模块‘参数0’。

定义显参以下代码
	显示先如果存在“参数00”【上】那么“‘参数00【上】’--”了
		‘参数【上】’
		先如果‘参数数目’那么“--‘参数’”了换行。
上代码。
定义通显参以‘显参’下代码
	‘参数栈’
上代码。

（
最后一个为缺省处理代码，下代码
	分叉‘名’先
	了
上代码
）
定义形参、前号、起【隔】以下代码
	别名“缺省”以“参数”‘参数数目’。
	循环【‘参数数目’】【次】先
		分支‘次’先
			“1”、‘参数数目’再来。
		了。
		别名“名”以“参数”‘次’。
		别名‘名’【上】以“参数”算术‘前号’“+‘次’-1”。
		别名“值”以‘名’。
		如果‘缺省’不等于“”那么
		分叉‘值’先
			、“-”、“=”赋予‘名’【上】以解释‘缺省’
		了。
	了
上代码。
